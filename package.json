{
  "name": "overthrone-prototype",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "verify": "vite build && node prototype/scripts/rules-check.mjs && node prototype/scripts/run-with-server.mjs"
  },
  "dependencies": {
    "three": "^0.185.1",
    "vite": "^8.1.2"
  },
  "devDependencies": {
    "playwright": "^1.57.0"
  }
}
